#!/bin/bash

export ANSIBLE_FORCE_COLOR=1
export PY_COLORS=1

ansible-pull ansible/desktops.yml -U git@github.com:yeetypete/homelab.git -C laptop/gravis -i ansible/inventory/inventory.yml -e override_hosts=localhost -K "$@"
