#!/bin/bash

export ANSIBLE_FORCE_COLOR=1
export PY_COLORS=1

if test -z "$SSH_TTY"; then
  export SSH_AUTH_SOCK=~/.1password/agent.sock
fi

ansible-pull ansible/desktops.yml -U git@github.com:yeetypete/homelab.git -i ansible/inventory/inventory.yml -e override_hosts=localhost -K "$@"
